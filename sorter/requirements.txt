# Gemini API client (current SDK — NOT the deprecated google-generativeai)
google-genai>=0.3.0

# Camera capture + JPEG encoding
opencv-python>=4.9.0

# STS3215 smart servos, driven straight from the Pi's UART by actuators/sts_bus.py
# (no vendor SDK - the protocol layer is ours)
pyserial>=3.5

# GPIO access for the HX711 load cell and inductive metal sensor
gpiozero>=2.0.1
RPi.GPIO>=0.7.1; platform_machine == "aarch64" or platform_machine == "armv7l"
# Raspberry Pi 5: RPi.GPIO does not support it. gpiozero needs lgpio as its backend there
# (header pins are /dev/gpiochip4); with it installed gpiozero picks it up automatically.
lgpio>=0.2.2.0; platform_machine == "aarch64"

# .env loading
python-dotenv>=1.0.1

# Testing (optional but recommended — see tests/)
pytest>=8.0.0
